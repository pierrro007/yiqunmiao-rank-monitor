# 一群喵排行榜监控系统 · 全量代码审查报告

- **审查日期**：2026-07-28
- **审查范围**：`monitoring/` 目录全部源码
  - `monitor_fetch.py`（抓取）、`monitor_apply.py`（落库/变化判定）、`monitor_run.py`（编排）
  - `sync_dd.py`（中国侧当当采集+回传）、`feishu_push.py`（飞书推送）
  - `index.html`（前端）、`daily.yml`（CI）
  - `update_chart_data.py` / `chart-data.js` / 4 个 `.md`（疑似历史残留）
- **严重度分级**：🔴 严重 / 🟠 高 / 🟡 中 / 🟢 低

---

## 一、总体评估

系统功能完整、可运行，已上线且满足"4 次/日定时 + 变化落库 + 网页留底 + 商品链接 + 飞书推送"的需求。架构上"京东走云端 / 当当走中国侧"的双写者拆分思路合理，并用 Contents API 的 409 重试规避并发覆盖，是亮点。

但存在 **1 个真实分类 bug、1 个飞书推送覆盖缺口、1 个凭证明文风险、零自动化测试**，以及若干重复代码与文档漂移。建议优先处理 🟠 高 级问题。

---

## 二、发现的问题清单

### 🟠 高（High）

#### H1. 京东"小剧场普通版"被误判为"京东特典版"（静默数据错归）
- **位置**：`monitor_fetch.py:141-147`（`jd_book_key` 的 小剧场 分支）
- **问题**：
  ```python
  if "小剧场" in title or "一群喵小剧场" in title:
      if "京东特典版" in title: return "小剧场京东特典版"
      if "印签" in title:        return "小剧场印签版"
      return "小剧场京东特典版"   # ← 兜底错误地回到"京东特典版"
  ```
  任一不含"印签/京东特典版"关键词的京东小剧场标题（例如普通版）都会被归到 `小剧场京东特典版`。
  叠加 `monitor_apply.py:37` 的 `JD_KEYS` **根本没有"普通版"这一 key**，导致京东侧小剧场普通版的排名会**静默覆盖/混入**京东特典版，造成数据污染。
- **建议**：
  1. `jd_book_key` 普通版兜底改为返回独立的 `小剧场普通版`（或 `小剧场京东普通版`）；
  2. 在 `monitor_apply.py` 的 `JD_KEYS` 同步加入该 key；
  3. 若确认京东当前不上普通版，至少把兜底改成 `return None`（而非错归），避免污染。

#### H2. 中国侧（DD 模式）的飞书推送实际失效
- **位置**：`sync_dd.py:110-113` + `feishu_push.py:71-76`
- **问题**：`feishu_push.py` 读取同目录的 `result.json` 判断 `changed` 并推送；但 `sync_dd.py` 运行 `monitor_run.py` 时只用 `r.stdout` 截取值，**从未把结果写入 `result.json`**。因此当当侧（中国机器定时跑）一旦发生排名变化，飞书群**收不到通知**，只有京东侧（云端）的变动会推。这与"有变化就推送"的设计目标不符。
- **建议**：在 `sync_dd.py` 的 `run_dd()` 里把 `monitor_run.py` 的 stdout 落盘为 `result.json`（与云端 `tee result.json` 对齐），再调用 `feishu_push.py`；或直接给 push 函数传参而非依赖文件。

#### H3. GitHub PAT 以明文形式存在于自动化配置与脚本读取路径
- **位置**：`sync_dd.py:30`（`GH_TOKEN = os.environ.get("GH_TOKEN")`）+ 本地自动化 `automation-1785218316874` 的 prompt 中直接内嵌了 `ghp_...` 经典令牌
- **问题**：该令牌为 **classic PAT，拥有 `repo` 全量权限**（可读写 owner 下所有仓库）。以明文字符串写进自动化 prompt、又以环境变量在本地进程传递，存在泄露即全仓库可写的风险；且令牌已出现在本次对话上下文中。
- **建议**：
  1. **立即轮换该令牌**（GitHub → Settings → Developer settings → 撤销旧 token）；
  2. 改用**细粒度 token（仅本仓库 contents 写权限）**或仓库自带的 `GITHUB_TOKEN`（但 `GITHUB_TOKEN` 在本地自动化不可用，可改用本机钥匙串/密钥管理读取，避免明文落盘）；
  3. 切勿把令牌写进可提交的脚本或自动化 prompt；统一从安全的密钥存储注入。

#### H4. 全仓库零自动化测试（最复杂逻辑无回归保护）
- **位置**：整个 `monitoring/` 无 `tests/`、无 `pytest`、CI 仅 `py_compile`
- **问题**：`monitor_apply.py` 的数组对齐/部分抓取 carry-forward 逻辑（`174-196` 行）是最易错、最关键的代码，目前仅靠开发期一次性沙盒脚本验证，没有沉淀为可重复运行的测试。任何改动（如增删子榜、改对齐算法）都可能在无人察觉时破坏"数组与 dates 对齐"这一核心不变式。
- **建议**：建立 `tests/test_apply.py`，用 pytest 覆盖关键场景：① 首次运行；② 同日重复运行有/无变化；③ 仅京东运行后当当被 carry-forward 且数组长度对齐；④ 仅当当运行后京东被 carry-forward；⑤ 新增 book key 在同日首次写入。把 `py_compile` + `pytest` 加入 CI 步骤。

---

### 🟡 中（Medium）

#### M1. `rank-history.json` 每次运行都提交（meta 时间戳抖动造成空提交）
- **位置**：`monitor_apply.py:170-171`（每次重写 `updated_at`/`last_probe_date`）+ `daily.yml:41`（`git diff --cached --quiet` 判定）
- **问题**：即使排名无任何变化，`meta` 里的 `updated_at`(精确到分钟) 与 `last_probe_date` 必然变化 → 文件内容变化 → `git diff` 非空 → **每天 4 次都产生一次"chore: 每日排行榜更新"提交**。已证实：07-28 当天 `rank-history.json` 有 9 次此类提交。这既污染 git 历史，又人为制造与 DD 侧回传的 409 竞争。
- **建议**：① 仅在 `changed=True` 或 `changed_today` 状态真正变化时提交；② 或把"最后探测时间"等易变字段移出需要 diff 判断的提交范围（例如提交前对比"是否仅有 meta 时间戳变化"，是则跳过 commit）；③ 也可将 `updated_at` 精确到"天"或仅在变化时更新。

#### M2. 死代码 / 架构残留（旧方案未清理）
- **位置**：`chart-data.js`、`update_chart_data.py`、4 个 `.md`（`一群喵小剧场_当当排名.md` 等）
- **问题**：当前 `index.html` 通过 `fetch('./rank-history.json')` 直接取数（第 150 行），**完全没有引用 `chart-data.js`**；`update_chart_data.py` 解析的那些 `.md` 也是旧手动补录数据，不再被任何流程消费。三者属于早期"解析 markdown 画图"方案的残留，增加阅读困惑与维护负担，且 `chart-data.js` 中的示例数据（仅 1 天）还会误导。
- **建议**：确认无人依赖后删除这三个文件 + 4 个 `.md`；若 `.md` 需留作历史存档，移到 `archive/` 目录并在 README 注明。

#### M3. 子榜/分类配置在 3 个文件各复制一份
- **位置**：`monitor_fetch.py:38-48`、`monitor_apply.py:39-47`、`monitor_run.py:26-33`
- **问题**：`JD_SUBCATS` 与 `DD_SUBCATS` 被复制粘贴到三个模块。新增一个当当/京东子榜必须同步改 3 处，漏改任意一处都会造成**静默数据缺口**（抓取到了但落库/展示缺失）。
- **建议**：抽出一个单一配置模块（如 `config.py` 或 `boards.py`），三处统一 `from config import JD_SUBCATS, DD_SUBCATS, build_expected()`，并加一个启动自检：断言 `EXPECTED` 的 key 在三处一致。

#### M4. 落库对齐逻辑过度复杂、可读性差
- **位置**：`monitor_apply.py:174-196`
- **问题**：`while len(arr) < target_len-1: append` + `if is_new_day: append else 覆盖` + `if len>target_len: del` 三段式补齐，意图（保证每个数组长度 == len(dates)）被实现得很绕，后续维护者极易误改出错。
- **建议**：封装一个 `pad_to(arr, length, fill)` 工具函数，逻辑简化为"先按 active/非 active 取今日值 → 统一 `pad_to(arr, target_len, last_val)`"。配合 H4 的测试可安全重构。

#### M5. 前端子榜渲染逻辑重复（renderSub / renderSubDD）
- **位置**：`index.html:348-415`
- **问题**：`renderSub`（京东）与 `renderSubDD`（当当）两函数结构几乎完全一致，仅 board 前缀与分类表不同，违反 DRY。新增一个平台的子榜就要再复制一份。
- **建议**：抽象为 `renderSubTable(tbody, subcats, boardKeyFn)`，用数据驱动（分类列表 + board 名模板）消除重复。

#### M6. 文档/注释漂移
- **位置**：`monitor_fetch.py:6-8`、`monitor_apply.py:4-14`、`monitor_run.py:3-12`
- **问题**：多处 docstring 仍写"四主榜 + 京东 13 子榜"，未提已上线的当当子榜；`monitor_apply.py` 顶部注释示例也未含 `dd_best_动漫幽默` 等。新接手者会误以为子榜只有京东。
- **建议**：统一更新 docstring，说明现有 4 主榜 + 26 京东子榜 + 2 当当子榜（并可扩展）的真实结构。

---

### 🟢 低（Low）

#### L1. 潜在存储型 XSS（书名/链接经 innerHTML 注入）
- **位置**：`index.html:339-340`（主表）、`365`、`400`（子榜 chip）
- **问题**：书名 `displayName(k)` 与商品链接 `ddUrl/jdUrl` 直接拼进 `innerHTML` / `href`。书名源自第三方页面标题，理论上若存在构造标题可注入 HTML/属性。当前数据自产、影响有限（公开静态页、无鉴权），但仍建议防御。
- **建议**：用 `textContent` 建节点，或对插入值做 HTML 转义（书名）与 URL 校验（仅允许 `https://product.dangdang.com/` 与 `https://item.jd.com/` 前缀）。

#### L2. 用正则解析 HTML 脆弱
- **位置**：`monitor_fetch.py:174-230`（`_BOOK_RE` / `<li>` 解析）
- **问题**：`<li>`、`<a title>` 用正则提取，站点改版或属性顺序变化即静默失败（即使有重试也抓不到）。属于可维护性/健壮性低。
- **建议**：长期可换轻量 HTML 解析（如 `html.parser` 或 `lxml`），短期至少在"整页 0 命中"时明确打日志告警，便于及时发现抓取失效。

#### L3. `curl` 未显式开启 `--compressed`
- **位置**：`monitor_fetch.py:78-87`
- **问题**：若目标返回 gzip 压缩响应而未加 `--compressed`，`decode_html` 会拿到压缩字节导致乱码/空解析。当前侥幸可用，但属隐患。
- **建议**：`curl` 命令加 `--compressed`。

#### L4. `feishu_push.py` 用 urllib，与 `sync_dd.py` 用 curl 不一致
- **位置**：`feishu_push.py:53-59` vs `sync_dd.py:37-43`
- **问题**：`sync_dd.py` 注释明确说本环境 urllib 上传遇 SSL 中断故统一用 curl；但 `feishu_push.py` 仍用 urllib 直连 `open.feishu.cn`，在中国侧运行时可能重蹈 SSL 覆辙。
- **建议**：统一走 `curl`；或抽出公共 HTTP 工具。

#### L5. `pretty_board` 依赖字符串替换顺序
- **位置**：`feishu_push.py:28-36`
- **问题**：靠 `replace` 顺序做板名映射，当前正确但脆弱；若新增 `jd_sales_xx` 之类易踩顺序坑。
- **建议**：改为字典映射 `{原始板名前缀: 中文}`，更直观可维护。

#### L6. `fetch_jd_sub` 仅取第 1 页（100 名）
- **位置**：`monitor_fetch.py:284-299`
- **问题**：京东子榜只抓前 100 名，若某冷门分类子榜 >100 名仍有目标书会漏抓。因小众分类基本够用，标 Low。
- **建议**：如后续发现漏抓，可把 `pageSize` 提高或翻页。

#### L7. 仓库缺 README / 运行说明
- **位置**：整个仓库
- **问题**：无 README 说明"本地如何跑、各环境变量含义（FETCH_MODE/GH_TOKEN/FEISHU_WEBHOOK）、双写者架构为何如此"，协作门槛高。
- **建议**：补一份 `README.md`，把架构图、环境变量表、如何新增子榜写清楚（正好顺手修正 M6 的文档漂移）。

---

## 三、优先级行动建议

| 优先级 | 问题 | 动作 |
|---|---|---|
| 🔴 立即 | H3 令牌明文 | 轮换 PAT，改用细粒度/密钥存储，勿明文落盘 |
| 🟠 本周 | H1 京东小剧场错归 | 修兜底 + 补 key，避免数据污染 |
| 🟠 本周 | H2 DD 侧飞书失效 | sync_dd 落盘 result.json 再推送 |
| 🟠 本周 | H4 测试缺失 | 加 pytest 覆盖对齐不变量 |
| 🟡 下轮 | M1 空提交 | 仅在真正变化时提交 |
| 🟡 下轮 | M2/M3/M6 | 清死代码、抽配置模块、补文档 |
| 🟢 排期 | L1-L7 | 逐步加固 |

---

## 四、值得肯定的点

- 双写者架构（云端 JD / 中国侧 DD）+ Contents API 409 重试，设计巧妙地解决了"跨境抓取 + 并发写同一 JSON"的难题，且经生产验证（云端 JD 运行后当当子榜数据完好）。
- 以**商品 ID 为判重锚点** + `product-map.json` 持久化，使"书名改了也认得出同一本书"，思路扎实。
- `monitor_apply.py` 的 carry-forward（未抓取榜沿用前值）保证数组对齐，核心不变式正确（已多次沙盒验证通过）。
- CDN 境内兜底（jsdelivr → BootCDN）体现了对国内访问稳定性的实际考量。
